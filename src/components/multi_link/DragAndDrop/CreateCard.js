import React from "react";

function CreateCard({ id, card_type, onChange, onCreate }) {
    // card_type에 따라서 서로 다른 card를 렌더해줄 수 있도록.
    // cards 라는 props? 에다가 정보를 추가해주면 되는거 일듯.
    return (
        <div>
            {/* <input text=""/>
            <input text=""/> */}
        </div>
    )
}

export default CreateCard;